# RJCouriers

# RJCouriers — Monolith demo

Run: open `index.html` in a browser. (Optional: run a static server: `npx serve`)

Features:
- Create shipments (saved in localStorage)
- Track by ID
- Calculate rates
- Admin: advance shipment status
